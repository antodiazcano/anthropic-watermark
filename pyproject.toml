[project]
name = "anthropic-watermark"
version = "0.1.0"
description = "Educational seed and tournament text watermarking with a local language model"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.14.0",
    "dotenv>=0.9.9",
    "langchain-groq>=1.1.3",
    "numpy>=2.3.0",
    "pydantic>=2.13.4",
    "torch>=2.5.0",
    "transformers>=4.46.0",
]

[dependency-groups]
dev = [
    "bandit>=1.9.4",
    "black>=26.5.1",
    "complexipy>=7.0.1",
    "flake8>=7.3.0",
    "flake8-pyproject>=1.2.4",
    "mypy>=2.3.1",
    "pylint>=4.0.7",
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.12",
]

[tool.flake8]
max-line-length = 88
ignore = ["E203", "W503"]

[[tool.mypy.overrides]]
module = ["numpy.*", "torch.*", "transformers.*"]
ignore_missing_imports = true

[tool.ruff]
line-length = 88
